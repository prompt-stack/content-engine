# Docker Compose Ports Configuration
# Change these to avoid port conflicts with other services

# API port (using obscure port to avoid conflicts)
API_PORT=9765

# PostgreSQL port (using obscure port to avoid conflicts)
POSTGRES_PORT=7654

# Redis port (using obscure port to avoid conflicts)
REDIS_PORT=8765